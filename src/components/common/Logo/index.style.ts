// import mui module
import styled from '@emotion/styled';

export const LogoComponentStyle = styled('div')(({ theme }: any) => {
  return {
    width: '100%',

    '.logo-img': {
      width: '100%',
      borderRadius: '30px'
    }
  }
});